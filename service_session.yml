name: "communication_example"
root: <%= @settings["working_directory"]%>

startup_window: monitoring
windows:
  - monitoring:
      layout: even-horizontal
      panes:
        - watch "systemctl status example_receiver"
        - watch "systemctl status example_sender"
